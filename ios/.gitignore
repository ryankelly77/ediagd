App/build
# The SPM package builds to its own directory, which App/build does not cover.
# A device build leaves this behind and it showed up untracked.
App/CapApp-SPM/build
App/Pods
App/output
App/App/public
DerivedData
xcuserdata

# Cordova plugins for Capacitor
capacitor-cordova-ios-plugins

# Generated Config files
App/App/capacitor.config.json
App/App/config.xml
