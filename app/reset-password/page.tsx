import { ResetPasswordScreen } from "./ResetPasswordScreen";

export const metadata = { title: "Set a new password — EDIAGD" };

export default function ResetPasswordPage() {
  return <ResetPasswordScreen />;
}
